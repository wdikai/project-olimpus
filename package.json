{
  "name": "game-starter",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "npm run serve",
    "prestart": "npm i && npm run build",
    "serve": "lite-server",
    "build": "tsc",
    "build:w": "tsc -w",
    "dev": "concurrently \"npm run build:w\" \"npm run serve\" ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Kostya Grigor'ev",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^3.4.0",
    "lite-server": "^2.3.0",
    "typescript": "^2.6.2"
  }
}
